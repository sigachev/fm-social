apiVersion: v1
kind: Service
metadata:
  name: social
  namespace: dev
  labels:
    app: social
spec:
  type: ClusterIP
  selector:
    app: social
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
